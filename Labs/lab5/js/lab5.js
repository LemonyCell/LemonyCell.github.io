window.addEventListener('load', function(){
  const body = document.body;
  const input = new RadixSort();

  const inputContainer = document.querySelector('.input-container');
  input.html(inputContainer);

});
